export enum UserPositions {
  frontend = 'frontend',
  backend = 'backend',
  fullstack = 'fullstack',
  designer = 'designer',
  PM = 'PM',
  PO = 'PO',
  other = 'other',
}

export enum Status {
  BAD_REQUEST = 400,
}
